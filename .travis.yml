language: php

dist: trusty

php:
  - 5.4
  - 5.5
  - 5.6

matrix:
  include:
    - php: 5.3
      dist: precise

install:
  - composer install --dev

script:
  - mkdir -p build/logs
  - phpunit --coverage-clover build/logs/clover.xml Tests

#after_script:
#  - vendor/bin/coveralls -v
